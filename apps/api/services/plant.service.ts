/**
 * get user by id
 */
export const getAllPlants = () => {
  // * GET all plants with
  // ** metadata like name, battery statues etc.
  // ** last values from humidity, sun, and moisture incl. timestamp
  return 'all plants'
}

// * GET historical data from plant x

// * PUT new plant updates
// add current sun data on top of old data from *today*
// reset if day is over

// * GET miflora sensors who are not setuped
// sensor must be near server to get find my the miflora search function
// compare existing mac adreses in db with found miflora sensors
